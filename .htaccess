Options -Indexes
DirectoryIndex index.php

<FilesMatch "\.(sql|md|env)$">
Require all denied
</FilesMatch>

# Protect the .env file explicitly (it starts with a dot).

<Files ".env">
Require all denied
</Files>
